# LawAgentt 设计讨论记录

## 文档目的

这份文档用于持续记录 LawAgentt 项目的产品边界、架构判断、agentic harness 设计、工具系统设计，以及我和 Codex 讨论过程中形成的心得与决策。

它不是最终 PRD，也不是代码设计定稿，而是项目早期的“设计记忆”。后续每次产生重要判断，都应该追加到这里，避免重复争论和遗忘上下文。

## 项目初始定位

LawAgentt 是一个面向普通个人、小微企业经营者、个体工商户和自由职业者的法律自助助手。它通过结构化事实收集、法律资料检索、风险分析和文书草稿生成，帮助用户在生活和生产经营中的常见法律问题里理解风险、准备材料、形成下一步行动方案。

第一版不定位为律师工作台，也不定位为正式法律意见系统。

## 第一版用户

- 普通个人
- 小微企业经营者
- 个体工商户
- 自由职业者

第一版用户通常不是法律专业人士。他们需要的是：

- 理解自己遇到的问题大概属于什么法律关系
- 知道自己可能有哪些权利和风险
- 知道下一步应该收集什么证据、联系谁、走什么流程
- 生成一份可编辑的文书草稿

## 第一版服务场景

第一版按全场景法律自助助手设计，但高频场景会做 Skill/场景包包装。正式业务场景包括：

- 劳动纠纷：拖欠工资、违法辞退、未签劳动合同、加班费
- 租房纠纷：押金、维修、提前退租、租赁合同争议
- 保险理赔：拒赔、少赔、理赔材料、保险合同条款争议
- 消费维权：退款、虚假宣传、商品或服务质量问题、平台投诉

设计规则：

```text
共享底座覆盖多场景通用流程：事实收集、法律/案例检索、证据门控、风险分析、行动建议和文书草稿。
高频场景通过 Skill 包增强：场景识别、FactProfile、争点模板、检索策略、计算规则、文书模板和 Review 条件。
```

暂不优先支持：

- 刑事案件
- 大额复杂商事诉讼
- 涉外法律
- 医疗事故
- 婚姻家事深度纠纷
- 需要律师代理的复杂诉讼策略

## 第一版输出

- 法律问答：用通俗语言解释问题
- 风险分析：说明不确定因素和潜在风险
- 行动建议：给出沟通、取证、投诉、仲裁、起诉等下一步路径
- 证据清单：列出用户应准备的材料
- 文书草稿：生成投诉书、律师函草稿、劳动仲裁申请书、民事起诉状初稿等

所有文书输出都应明确标注：

- 文书是草稿
- 事实来自用户提供
- 法律依据来自检索结果
- 提交前建议人工核对或咨询律师

## 明确不做

- 不承诺胜诉或具体法律结果
- 不替代律师提供正式法律意见
- 不编造事实、证据、时间、金额、身份信息
- 不生成伪造证据、虚假陈述、恶意投诉
- 不处理刑事辩护、重大人身伤害、重大财产纠纷等高风险案件
- 不在缺少关键事实时给确定性结论
- 不在没有法律来源支撑时输出确定法律依据
- 不直接代表用户提交材料或联系第三方，除非后续有明确授权流程

## 地区信息设计决策

中国大陆全国性法律规则默认一致，因此地区不应该作为所有咨询流程的强制入口字段。

但地区仍应作为可选字段保留，因为以下场景可能需要地区信息：

- 赔偿金额计算
- 管辖机构判断
- 地方标准或地方裁判口径检索
- 正式文书抬头生成
- 需要提交、投诉、仲裁或起诉路径

设计规则：

```text
系统默认适用中国大陆全国统一法律。
只有当用户需要金额测算、管辖机构、地方标准或正式文书抬头时，才追问省市信息。
```

## 架构范式决策

基于 Zero2Agent 架构选型页的框架，LawAgentt 不应设计为纯 ReAct Agent。

法律场景风险高，不能让模型自由决定是否跳过事实收集、是否检索、是否生成文书。第一版应采用：

```text
外部状态机 / Workflow 控制流程
+
LLM 在每个节点内做局部智能
+
关键输出由 Review / Guardrail 节点检查
```

推荐范式：

```text
Plan-and-Execute 主干
+ ReAct 局部检索与补查
+ Reflexion / Review 关键输出审核
```

## 第一版 Agentic Harness 状态机草案

```text
START
→ collect_initial_problem
→ classify_matter
→ check_supported_scope
→ extract_facts
→ check_missing_facts
→ ask_followup OR retrieve_sources
→ analyze_legal_position
→ generate_action_plan
→ generate_document_draft
→ review_output
→ deliver_result
→ END
```

重要原则：

- Orchestrator 是唯一流程控制者
- 每个 agent 是受控节点，不自由聊天协作
- 每个节点输入输出都应有 JSON Schema
- 每一步都落库
- 每一步都可重放
- 每一步都可审计

## 基于 Tool Management 页的工具系统设计

Zero2Agent 工具管理页强调：生产级 Agent 的核心问题不是“会不会 function calling”，而是模型不听话怎么办，以及工具规模变大后怎么管理。

LawAgentt 的工具系统需要从第一天就做成可控的工程系统，而不是把所有工具 schema 塞给模型。

### 工具调用三层防线

1. Prompt / Schema 引导
   - 工具描述必须写清楚适用场景
   - 参数描述必须包含反面说明
   - 例如：`caseFacts` 只能来自用户输入，不得由模型补造

2. 硬校验
   - 使用 Pydantic / Zod / JSON Schema 校验工具参数
   - 类型错误、枚举错误、缺失关键字段时拒绝调用
   - 将具体错误反馈给模型重试

3. 业务兜底
   - 关键参数必须做业务校验
   - 例如文书生成前必须确认当事人身份、请求事项、事实依据、证据字段
   - 未通过校验时进入追问或降级，而不是直接生成

### 工具路由设计

不要让模型在全量工具中选择。采用分层路由：

```text
第一层：业务意图分类
劳动纠纷 / 租房纠纷 / 保险理赔 / 消费维权 / 合同审查 / 公司事务 / 不支持场景

第二层：工具大类选择
事实抽取 / 法律检索 / 案例检索 / 模板检索 / 金额计算 / 文书生成 / 风险审核

第三层：具体工具召回
在对应大类里通过关键词、规则、向量检索召回 top 3-5

第四层：最终选择
只把 top 1-2 个工具的完整 schema 交给模型或执行器
```

这样避免百级工具库下的召回偏差和上下文爆炸。

### 第一版工具大类

- `fact_tools`
  - 事实抽取
  - 缺失事实检查
  - 证据字段归一化

- `legal_retrieval_tools`
  - 法律法规检索
  - 司法解释检索
  - 地方规则检索
  - 案例检索

- `document_tools`
  - 模板检索
  - 文书字段填充
  - docx / pdf 导出
  - 文书版本管理

- `calculation_tools`
  - 工资拖欠金额计算
  - 赔偿金 / 补偿金初步测算
  - 诉讼费或仲裁相关费用估算

- `review_tools`
  - 引用覆盖检查
  - 编造事实检查
  - 高风险场景识别
  - 输出合规检查

### Tool Adapter / Semantic Wrapper

所有工具返回都必须经过中间层清洗，不把原始接口结果直接喂给模型。

统一返回结构建议：

```json
{
  "status": "success | partial | empty | error",
  "data": {},
  "normalizedData": {},
  "confidence": 0.0,
  "source": {
    "toolName": "",
    "retrievedAt": "",
    "sourceIds": []
  },
  "warnings": [],
  "error": null
}
```

法律工具尤其要区分：

- 查了但没有结果
- 没有查
- 工具失败
- 找到结果但置信度低
- 找到多个冲突结果

### 工具失败与降级

工具调用失败时不允许无限重试。

建议策略：

- 格式错误：规则修复一次
- 参数非法：带具体错误让模型重试一次
- 工具超时：重试一次，然后降级
- 检索为空：改写 query 后重试一次
- 仍失败：向用户说明无法确认，或进入人工/律师建议

### Mock 与测试

法律 agent 的测试必须大量使用 mock：

- Mock 法规检索结果
- Mock 案例检索结果
- Mock 用户事实输入
- Mock 文书模板
- Mock 工具超时、空结果、冲突结果

目标不是测试外部数据库本身，而是测试 harness 在不同工具返回情况下是否稳定：

- 是否追问缺失事实
- 是否拒绝无来源结论
- 是否避免编造事实
- 是否正确生成证据清单
- 是否在高风险场景降级

## 基于 Memory & Context 页的记忆系统设计

Zero2Agent 记忆与上下文页强调，记忆系统不是简单“接一个向量数据库”，而是要回答：存什么、怎么查、怎么融合。对于 Agent 场景，最可靠的方案是把关键状态外置，而不是依赖模型在长上下文里记住。

LawAgentt 当前不应一次性定死完整记忆系统实现，但需要先确定记忆分类、读写边界和可扩展接口。

### 当前决策

现在先不决定具体向量库、图数据库、摘要算法或长期记忆策略。

现在必须先决定：

- 哪些信息属于任务状态，必须结构化存储
- 哪些信息属于对话上下文，可以摘要或压缩
- 哪些信息属于长期记忆，需要用户授权和版本管理
- 每个 agent 节点能读什么、能写什么
- agent 之间只传结构化结论，不传推理过程

### 第一版记忆分层

1. 请求级感知记忆
   - 当前用户输入
   - 当前上传文件
   - 当前节点可见的工具结果
   - 生命周期：单次请求

2. Matter 工作记忆
   - 一个法律事项内的结构化状态
   - 例如事实字段、证据字段、已完成步骤、检索结果、文书版本
   - 生命周期：一个案件/事项
   - 这是第一版最重要的记忆层

3. 会话摘要记忆
   - 多轮对话的结构化摘要
   - 只保留用户目标、已确认事实、缺失信息、当前进展
   - 不作为法律事实的唯一来源

4. 长期用户记忆
   - 用户偏好、常用身份信息、历史事项索引
   - 第一版只预留接口，不默认启用强个性化长期记忆
   - 涉及隐私和法律风险，必须有用户授权、可查看、可删除

### 法律场景中的硬性规则

- 法律事实不能只存在对话摘要里，必须进入 `FactProfile`
- 文书字段不能从长期记忆自动填入，除非用户确认
- 用户身份、金额、日期、证据等硬字段不得被摘要压缩替代
- Review Agent 不能读取其他 agent 的隐藏推理，只读取结构化输出和证据
- 多 agent 之间传递结论，不传递过程

### 节点级记忆设计方式

后续设计每个节点时，都要回答四个问题：

```text
1. 这个节点需要读取哪些记忆？
2. 这个节点会写入哪些结构化状态？
3. 这些状态的生命周期是什么？
4. 哪些内容必须用户确认后才能进入长期记忆？
```

这意味着记忆系统会随着节点设计逐步展开，但底层必须统一成 `MemoryStore / MatterState / FactProfile / AgentRun` 等接口，避免每个 agent 私自维护上下文。

## 基于 Multi-Agent Collaboration 页的多智能体设计

Zero2Agent 多智能体协作页强调，多 agent 的核心不是“多个模型互相聊天”，而是角色隔离、通信规范、冲突收敛。判断是否需要多 agent 不能只看任务复杂度，而要看：

- 能力边界是否清晰
- 上下文是否容易污染
- 是否存在天然并行

### 当前决策

LawAgentt 正式采用“受控多 agent + 多 agent 结构化通信”，不采用自由对话式多 agent。

```text
Orchestrator / Workflow 是唯一流程控制者
各 Agent 是受控节点
Agent 之间不自由聊天
Agent 之间只传结构化结论，不传推理过程
共享状态由 MatterState / Blackboard 管理
```

### 为什么需要多 agent

法律咨询与文书生成包含明显不同的能力边界：

- 事实收集与缺失事实追问
- 法律资料检索
- 法律分析
- 文书草稿生成
- 引用与风险审核

这些环节的失败模式不同，拆开后更容易精准测试、重试和降级。

### 第一版 Agent 角色

- `IntakeAgent`
  - 负责理解用户问题、抽取事实、识别缺失字段
  - 不能生成最终法律结论

- `RetrievalAgent`
  - 负责构造检索 query、调用法规/案例/模板检索工具
  - 不能基于未检索内容直接下结论

- `AnalysisAgent`
  - 负责基于已检索来源生成结构化法律分析
  - 必须绑定引用来源

- `DraftingAgent`
  - 负责基于模板和已确认事实生成文书草稿
  - 不能补造事实字段

- `ReviewAgent`
  - 负责检查引用缺失、事实编造、高风险场景、文书字段完整性
  - 不能直接修改事实，只能提出问题或要求重做

- `Orchestrator`
  - 负责状态机流转、权限控制、重试、降级、冲突仲裁
  - 是唯一能推进全局状态的组件

### 通信协议原则

Agent 之间不传自然语言长文，而传结构化消息：

```json
{
  "matterId": "",
  "fromAgent": "RetrievalAgent",
  "toAgent": "AnalysisAgent",
  "messageType": "retrieval_result",
  "payload": {},
  "confidence": 0.0,
  "sourceIds": [],
  "warnings": []
}
```

`Multi-Agent Communication v0` 已在 `lawagent_runtime/` 中实现：

- `AgentMessage` 表示 Agent 之间的结构化消息。
- `StatePatch` 表示 Agent 对共享状态的受控修改请求。
- `Observation` 表示节点执行结果，可携带消息和状态补丁。
- `apply_authorized_patch` 由 Orchestrator/Runtime 使用，先做角色权限校验，再应用补丁。
- v0 中 Orchestrator 是唯一可写流程控制字段的角色；业务 Agent 只能按权限追加状态，不能自由修改全局状态。

`Node/Router/AgentRuntime v0` 已在 `lawagent_runtime/` 中实现：

- `AgentNode` 是节点协议，节点接收 `RunState` 并返回 `Observation`。
- `NodeRegistry` 负责节点注册和查找。
- `DefaultRouter` 采用规则优先路由，不让模型自由决定流程跳转。
- `AgentRuntime` 负责执行节点、应用授权补丁、记录 trace、处理预算和失败。
- 当前已能通过 stub 节点跑通 intake → plan → retrieve → answer，并覆盖澄清、越权补丁和缺失节点失败路径。

### 冲突收敛

多 agent 不允许无限来回。

- 单节点最多重试 1-2 次
- Review 失败时回到明确的上一节点，而不是全局重跑
- 检索结果冲突时标注冲突，不让模型假装唯一结论
- 高风险冲突交给规则或人工升级
- Orchestrator 拥有最终仲裁权

### 与旧设计的冲突处理

此前项目记忆中存在“第一阶段暂用一个编排 Agent，多 Agent 不作为 MVP 必需项”的表述。2026-08-04 用户明确确认：项目采用多 Agent + 多 Agent 通信。处理结果：

```text
旧表述不再作为产品/架构方向。
新基线是 Orchestrator 主控的受控多 Agent。
多 Agent 通信必须结构化、可审计、可重放。
自由聊天式多 Agent 仍然明确不采用。
```

### 共享状态

采用黑板模式，但要做读写权限隔离：

- `FactProfile`：IntakeAgent 写，ReviewAgent 可标记问题，其他 Agent 只读
- `RetrievalResults`：RetrievalAgent 写，AnalysisAgent / ReviewAgent 读
- `LegalAnalysis`：AnalysisAgent 写，DraftingAgent / ReviewAgent 读
- `GeneratedDocument`：DraftingAgent 写，ReviewAgent 读并给审核意见
- `ReviewResult`：ReviewAgent 写，Orchestrator 读并决定是否交付

共享记忆中只存结论和事实，不存推理过程。

## 基于 Fault Tolerance 页的容错与鲁棒性设计

Zero2Agent 容错与鲁棒性页强调，生产环境里的 Agent 必须假设工具会超时、模型会选错工具、RAG 会检索错、用户会输入攻击内容、系统会过载。LawAgentt 作为法律场景应用，必须从第一版就设计失败路径，而不是只设计成功路径。

### 当前决策

LawAgentt 的 Orchestrator 必须内置容错策略。每个节点都要声明：

- 可能失败类型
- 最多重试次数
- 是否允许参数修复
- 是否允许降级输出
- 是否需要用户确认
- 是否需要人工/律师升级
- 失败时写入什么审计日志

### 失败分类

第一版至少区分：

- `temporary_error`
  - 网络超时、模型 API 超时、检索服务 5xx
  - 策略：指数退避，最多 2 次

- `invalid_params`
  - 工具参数缺失、枚举错误、日期/金额格式错误
  - 策略：结构化错误回传给当前 Agent，允许修复 1 次

- `missing_facts`
  - 缺少关键事实，例如入职时间、工资金额、解除原因
  - 策略：回到追问用户节点

- `retrieval_empty`
  - 检索为空或低置信
  - 策略：query 改写后重试 1 次；仍失败则输出低置信说明，不给确定结论

- `retrieval_conflict`
  - 检索结果之间冲突
  - 策略：显式标注冲突，禁止合成为单一确定结论

- `review_failed`
  - 引用缺失、事实编造、文书字段不完整、越界法律意见
  - 策略：回到对应上一节点修复；最多 1-2 次

- `high_risk_case`
  - 刑事、重大财产、人身伤害、复杂诉讼策略
  - 策略：停止自动交付确定意见，建议律师/人工介入

### 幻觉治理补丁

LawAgentt 不能只靠 RAG 防幻觉。法律场景需要多层防线：

```text
生成前：事实结构化 + RAG 检索 + 上下文精简
生成中：JSON Schema + 引用绑定 + 禁止无来源结论
生成后：ReviewAgent 校验事实、引用、金额、文书字段
高风险：人工/律师升级
```

特别要防：

- 知识空白幻觉：没有法律来源却给结论
- 检索失败幻觉：基于不相关法规回答
- 合成幻觉：把多个来源之间不存在的关系编成结论
- 工具幻觉：伪造工具调用或工具返回
- 状态幻觉：把用户没说过的事实当成事实
- 文书幻觉：补造日期、金额、身份、证据

### 误操作与写操作防护

LawAgentt 第一版虽然不直接提交法律材料，但仍会有写操作：

- 保存案件事实
- 保存文书草稿
- 导出文件
- 后续可能发送邮件、提交投诉、联系律师

所有写操作要支持：

- dry-run / preview
- 用户确认
- 审计日志
- 可回滚或保留版本

默认原则：

```text
模型可以生成建议和申请，但不能直接执行高风险外部操作。
高风险操作默认不执行，必须用户显式确认。
```

### 循环与预算限制

每个 Matter 运行必须有预算：

- 全局最大节点数
- 单工具连续失败上限
- 单节点重试上限
- 单任务 token / 成本预算
- 总超时时间

行为检测：

- 连续重复同一工具 + 同一参数 → 禁用该路径
- 连续无信息增益 → 降级输出或追问用户
- 当前动作偏离 Matter 目标 → 回到 Orchestrator 重新锚定

### Prompt Injection 与数据隔离

法律产品会处理用户上传材料、网页、聊天记录、合同文本。这些材料可能包含恶意指令。

设计要求：

- 用户输入和系统指令必须隔离
- RAG 结果标记为“参考材料”，不是指令
- 上传文档中的指令性文本不能改变 agent 行为
- 输出前检查是否泄露系统 prompt、密钥、用户隐私
- 工具权限按 agent 角色绑定

### 资源过载与降级

生产环境需要预留资源紧张策略：

- 正常：完整流程，包含 Review
- 轻度过载：使用小模型处理分类、事实抽取
- 中度过载：减少检索 topK、减少反思次数
- 重度过载：只提供初步问答，不生成文书
- 极端过载：排队或返回 429 / Retry-After

用户侧应明确提示当前模式，例如“快速模式”“完整分析模式”。

## 当前下一步

下一步应围绕“共享底座 + 高频场景 Skill”继续细化。建议先选一个高频场景作为 Skill 包样板，但通用 Runtime、Tool、Retrieval 和 Evidence Gate 不写死为单一场景：

1. 定义通用 `MatterProfile / FactProfile` 基础字段
2. 定义劳动纠纷、租房纠纷、保险理赔、消费维权的 Skill 包边界
3. 选择一个高频场景作为首个 Skill 样板，定义其 FactProfile Schema
4. 定义该 Skill 的工具列表和工具路由规则
3. 定义法律检索返回格式
4. 定义文书生成前的硬校验条件
5. 定义 Review 节点检查项

## 讨论日志

### 2026-08-04

- 明确 LawAgentt 第一版服务普通个人、小微企业、个体工商户、自由职业者。
- 初步确定第一版场景优先级：劳动纠纷、租房纠纷、消费维权。
- 二次确认产品不做单一首发场景，而是正式业务包含劳动纠纷、租房纠纷、保险理赔、消费维权等；底层按全场景通用能力建设，高频场景通过 Skill/场景包包装。
- 明确系统不替代律师、不承诺结果、不生成虚假事实、不处理刑事或高风险案件。
- 基于 Zero2Agent 架构选型页，决定采用 workflow 控制主流程，而不是纯 ReAct。
- 讨论地区字段：全国法律默认一致，地区不作为入口强制字段，但在金额计算、管辖机构、地方标准、文书抬头等场景按需追问。
- 基于 Zero2Agent 工具管理页，决定工具系统采用分层路由、参数硬校验、semantic wrapper 和有限重试降级策略。
- 基于 Zero2Agent 记忆与上下文页，决定暂不定死完整记忆系统实现，但先确定记忆分层、读写边界和节点级记忆设计问题。第一版重点是 Matter 工作记忆和结构化 FactProfile，而不是泛化长期记忆。
- 基于 Zero2Agent 多智能体协作页，决定 LawAgentt 使用受控多 agent：按法律任务阶段拆分角色，但由 Orchestrator/Workflow 统一控制流程，不采用自由聊天式多 agent。
- 二次确认项目正式采用多 Agent + 多 Agent 通信；旧的“多 Agent 暂不作为 MVP 必需”表述废止，保留 Orchestrator 主控、结构化通信和冲突收敛边界。
- 基于 Zero2Agent 容错与鲁棒性页，补充失败分类、重试预算、误操作防护、幻觉治理、Prompt Injection 防护和资源过载降级策略。LawAgentt 必须设计失败路径，而不只设计成功路径。

## 参考资料

- Zero2Agent：架构选型：ReAct、Plan-and-Execute 与 ToT 怎么选  
  https://onefly.top/zero2Agent/learn-agent-interview/01-architecture-design/index.html
- Zero2Agent：工具管理：参数校验、工具路由与百级工具库  
  https://onefly.top/zero2Agent/learn-agent-interview/02-tool-management/index.html
- Zero2Agent：记忆与上下文：长对话不丢信息的实战方案  
  https://onefly.top/zero2Agent/learn-agent-interview/04-memory-context/index.html
- Zero2Agent：多智能体协作：角色分工、通信机制与冲突仲裁  
  https://onefly.top/zero2Agent/learn-agent-interview/06-multi-agent-collab/index.html
- Zero2Agent：容错与鲁棒性：超时、报错、误操作的工程化处理  
  https://onefly.top/zero2Agent/learn-agent-interview/03-fault-tolerance/index.html
