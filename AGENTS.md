# LawAgent 协作与记忆规则

本文件适用于整个仓库。任何新任务、子任务或恢复开发的窗口，在修改代码或数据前必须按顺序完成：

1. 阅读根目录 `MEMORY.md`，恢复项目目标、已确认决策、当前阶段、风险和用户偏好。
2. 阅读根目录 `DEVELOPMENT.md`，恢复架构、数据契约、环境、开发流程和当日进度。
3. 阅读根目录 `requirement.txt` 与 `requirements.txt`，核对实际环境和目标依赖。
4. 检查工作区实际状态；文档与代码/数据冲突时，以可验证事实为准，并同步修正文档。

## 沟通与进度

- 使用简体中文，先给结论，再说明关键依据。
- 开始工作时说明本次目标、当前阶段和准备执行的最小开发切片。
- 持续工作期间及时报告关键发现、风险、假设和验证结果。
- 完成一个可验证切片后更新 `MEMORY.md` 的当前阶段，并更新 `DEVELOPMENT.md` 的进度。
- 追加文档前必须先检索同主题的现有表述，判断是补充、替换还是冲突；发现冲突时直接合并修正并说明决策状态，禁止只在文末叠加新说法。
- 不把完整对话逐字写入记忆；只保留可复用事实、决策、原因、结果、风险和下一步。
- 用户正在学习如何借助 Codex 完成项目。遇到任务范围不清、授权边界、验收条件、测试方法、上下文管理或任务拆分等高价值节点时，主动给出一条简短的“Codex 使用提示”和可直接复用的示例提问；不要在每轮机械教学或打断开发。
- 简单、低风险且已明确授权的任务可以直接执行。但当前项目功能开发采用“设计确认门控”：先通过聊天明确目标、必要模块、接口、数据流、最小原型和验收标准；用户明确确认设计后，才能修改相应业务代码。
- 只读检查、数据画像和设计文档整理不等同于实现授权；不得因为设计已经详细就自行进入开发。

## 开发环境

- 当前标准环境：Conda `agent`，Python 3.11。
- 当前受限终端中 `conda run` 可能因 Conda 环境目录只读而无法创建临时文件；此时直接使用 `/root/miniconda3/envs/agent/bin/python`，并核验解释器属于 `agent` 环境。
- 路径、设备、模型、数据库和密钥全部配置化；禁止新增服务器绝对路径或明文密钥。
- 当前服务器无 GPU，代码必须支持 CPU；GPU 优化只能作为可选能力。

## 已确认架构边界

- Python + FastAPI + Pydantic v2。
- MVP 不使用 LangGraph；采用自研轻量 Agent Runtime / State / Node / Router 承载多跳 Agentic 工作流。LangGraph 仅保留为后续对照实验或可替换参考，不作为当前实现依赖。系统必须根据中间证据动态决定下一跳，不能退化为“检索一次后直接生成”的单跳 RAG。
- Qdrant 承载向量和 payload 检索。
- LLM 不直接生成数据库过滤表达式；模型输出结构化意图槽位，由代码校验并构造 filter。
- 检索结果必须融合去重并经过 rerank，回答必须绑定可追溯证据。
- 法律安全优先于回答覆盖率：证据不足、时效/法域不明、来源冲突或关键事实缺失时，必须澄清、有限回答或拒绝下结论，并给出用户可执行的补充材料建议。
- Agentic Retrieval 是项目核心卖点：问题分解/查询改写 → 检索 → rerank → 证据充分性与缺口判断 → 定向补检索 → 生成与引用校验。
- 采用 Orchestrator 主控的受控多 Agent 架构，并设计多 Agent 结构化通信；多 Agent 不代表自由聊天协作，Orchestrator 是唯一能推进全局状态、权限控制、重试、降级和冲突仲裁的组件。
- 用户案件记忆、检索证据和公共法律知识必须分层隔离，任何对话推断都不得写回公共知识库。
- 本地 BGE-M3 是当前 embedding 候选；reranker、生成模型、CRAG 和 Step-back 的最终采用均需以实验和明确决策为准。

## 数据操作纪律

- 原始数据默认只读。修正、清洗和转换输出到版本化派生目录，不原地覆盖。
- 数据处理必须可重复、可审计、可断点续跑，并生成 manifest。
- 任何修正、删除、移动、打包或解包操作前先统计目标范围并保留校验信息。
- 每次数据处理都在 `DEVELOPMENT.md` 记录：输入、输出、schema/脚本版本、命令、数量、大小、哈希、异常、验证和恢复方法。
- 数据与模型默认不提交 Git；仅提交 schema、脚本、小型脱敏样例和 manifest 模板。

## 收尾与任务交接

任务结束前必须保证后续窗口只读以下文件即可恢复上下文：

- `AGENTS.md`：自动启动规则与硬约束。
- `MEMORY.md`：长期目标、决策、偏好、当前状态和下一步。
- `DEVELOPMENT.md`：可执行架构、数据处理记录与开发日志。
- `requirement.txt`：实际环境及依赖变更台账。

若本次工作没有改变项目事实，不为“留痕”而制造无意义文档更新。
